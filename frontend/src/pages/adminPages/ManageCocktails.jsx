// Interfaces for creating, updating, and deleting cocktails.
// Separate sections or forms for adding a new cocktail (`POST`), updating (`PUT`), and deleting (`DELETE`) cocktails.
// Back to dashboard button

export const ManageCocktails = () => {


    return (
        <div>
            <h1>Cocktail Management</h1>
        </div>
    )
}